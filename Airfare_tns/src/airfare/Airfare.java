package airfare;
public interface Airfare {
    // Calculate and return the amount (using wrapper Double as specified)
    Double calculateAmount();

    // Display method declaration
    void display();
}
